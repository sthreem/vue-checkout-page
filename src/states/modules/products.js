// Initial state
const initialState = () => {};

// Set initial state
const state = initialState();

// Getters - Returns computed state properties
const getters = {};

// Actions - Asynchronous code or business logic that commits mutations
const actions = {};

// Mutations - Synchronous modifications of the state's object
const mutations = {};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
};
